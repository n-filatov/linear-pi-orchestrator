# Linear integration

Transport-only MCP client support for Linear. Provider selector semantics live
in the Linear trigger package, so other actions can share the client without
depending on a trigger implementation.

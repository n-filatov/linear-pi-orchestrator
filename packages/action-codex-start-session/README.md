# Codex start-session action

Starts a Codex App Server worker and can attach it to a tmux action. Provide
exactly one of `prompt` or `promptFile`; output includes the worker endpoint,
thread, and turn identifiers when available.

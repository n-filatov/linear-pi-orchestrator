# Plugin host

Owns trusted external plugin loading, package installation and lockfile health.
It depends only on the public plugin SDK; the application composes built-ins and
runtime clients separately.

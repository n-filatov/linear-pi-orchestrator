/**
 * Compatibility surface while callers move to @task-relay/global-registry.
 * The cross-checkout registry implementation lives in the workspace package.
 */
export * from "@task-relay/global-registry";

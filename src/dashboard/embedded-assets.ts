// The native compiler replaces this module with the built dashboard assets.
// Source and npm development use files from dist/dashboard-client instead.
export const embeddedDashboardAssets: ReadonlyMap<string, Uint8Array> = new Map();
